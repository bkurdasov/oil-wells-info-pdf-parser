## Custom pdf parser with web interface

Given date, it downloads pdf from dmr.nd.gov, converts it into csv and lets user download it. Used modules and frameworks: **Flask**, **slate**, **bootstrap**.

